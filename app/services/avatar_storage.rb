# frozen_string_literal: true

module AvatarStorage
  # Base module for avatar storage implementations.
  # Different storage backends can be implemented as subclasses
  # under this module (e.g., AvatarStorage::S3, AvatarStorage::Filesystem).
end
