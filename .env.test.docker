# Test Environment Configuration (Docker)
# Use this file when running tests via Docker Compose

# Database credentials
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres

# LocalStack S3 configuration (Docker service name)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_REGION=us-east-1
AWS_ENDPOINT_URL=http://localstack:4566
AWS_FORCE_PATH_STYLE=true
AVATAR_S3_BUCKET=user-avatars

# Redis (Docker service name, use database 2 for test isolation)
REDIS_URL=redis://redis:6379/2
